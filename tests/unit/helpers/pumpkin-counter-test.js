import { pumpkinCounter } from '../../../helpers/pumpkin-counter';
import { module, test } from 'qunit';

module('Unit | Helper | pumpkin counter');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pumpkinCounter(42);
  assert.ok(result);
});
